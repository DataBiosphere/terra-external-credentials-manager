// Spotless requires this at the root level
buildscript { repositories { mavenCentral() } }

plugins {
	id 'idea'
}

allprojects {
	group = 'bio.terra'
	version = '0.10.0-SNAPSHOT'
}

idea.project.jdkName = '11'
