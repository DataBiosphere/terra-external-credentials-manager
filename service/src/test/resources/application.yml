externalcreds.providers.services:
  fake-provider:
    clientId: fake-id
    clientSecret: fake-secret
    linkLifespan: 15d
    openidConfiguration: https://fake-config-url
    revokeEndpoint: https://fake-revoke-endpoint?token=%s

spring:
  application.name: externalcreds-test

  datasource:
    hikari.connection-timeout: 5000
    password: ${DATABASE_USER_PASSWORD:ecmpwd}
    url: jdbc:postgresql://${DATABASE_HOSTNAME:localhost}:5432/ecm
    username: ${DATABASE_USER:ecmuser}

  main.banner-mode: off
